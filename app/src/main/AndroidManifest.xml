<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scheduleapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".firebase.FirebaseService"
            android:enabled="true"
            android:exported="true"></service>

        <activity android:name=".AfterLoginActivity" />
        <activity
            android:name=".ChangeEmailActivity"
            android:theme="@style/noTitleDialog" />
        <activity
            android:name=".CheckPwActivity"
            android:theme="@style/noTitleDialogRound" />
        <activity
            android:name=".MyInfoActivity"
            android:theme="@style/noTitleDialog" />
        <activity
            android:name=".SelectedDayPage"
            android:theme="@style/noTitleDialogRound" />
        <activity android:name=".GroupSchedulePage" />
        <activity
            android:name=".MemberActivity"
            android:theme="@style/noTitleDialog" />
        <activity android:name=".InvitePage" />
        <activity
            android:name=".CreateGroupPage"
            android:theme="@style/noTitleDialogRound" />
        <activity
            android:name=".AddFriendPage"
            android:theme="@style/noTitleDialogRound" />
        <activity
            android:name=".SInputPage"
            android:theme="@style/noTitleDialogRound" />
        <activity
            android:name=".FindPage"
            android:theme="@style/noTitleDialogRound" />
        <activity
            android:name=".ChangePwPage"
            android:theme="@style/noTitleDialog" />
        <activity android:name=".JoinPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>